spring:
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/migration/test
  datasource:
    username: 'group4'
    url: jdbc:mysql://localhost:3316/bookstore
    password: 'password'